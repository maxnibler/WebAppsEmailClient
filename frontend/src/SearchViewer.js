
import {Box} from '@material-ui/core';
import React from 'react';

/**
 * @param {function} setSearch
 * @return {JSX}
 */
function searchViewer(setSearch) {
  return (
    <Box>
      search
    </Box>
  );
}

export default searchViewer;
